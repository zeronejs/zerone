{
  "name": "parcel-html-template",
  "version": "1.0.0",
  "description": "",
  "source": "src/index.html",
  "scripts": {
    "dev": "npm run prebuild && parcel ./src/*.html",
    "prebuild": "rimraf .parcel-cache && rimraf dist",
    "build": "npm run prebuild && parcel build ./src/*.html",
    "build:prod": "npm run prebuild && parcel build ./src/*.html --public-url ./",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "parcel": "^2.9.2",
    "postcss": "^8.4.21",
    "prettier": "^2.8.4",
    "prettier-plugin-tailwindcss": "^0.2.4",
    "rimraf": "^3.0.2",
    "tailwindcss": "^3.2.4",
    "typescript": "^5.1.3"
  }
}
