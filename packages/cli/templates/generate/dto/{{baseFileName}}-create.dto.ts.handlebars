import { IsNotEmpty, IsOptional } from 'class-validator';
import { ApiProperty } from '@nestjs/swagger';
export class {{BaseName}}CreateDto {
	{{#each property}}
    {{#unless isSpecialColumn }}
    /**
     * {{documentation.[0].text}}
     */
    @ApiProperty({
        description: '{{documentation.[0].text}}',
        required: {{#unless isOptional }}true{{else unless isRead}}false{{/unless}},
    })
    {{#if isOptional }}
    @IsOptional()
    {{escapedName}}?: {{{type}}};

    {{else}}
    @IsNotEmpty({
        message: '{{documentation.[0].text}}不能为空',
    })
    {{escapedName}}: {{{type}}};

    {{/if}}
    {{/unless}}
	{{/each}}
}
