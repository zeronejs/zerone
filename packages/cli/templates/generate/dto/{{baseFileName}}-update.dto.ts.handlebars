import { IsOptional } from 'class-validator';
{{#each dotImports}}
{{{this}}}
{{/each}}
export class {{BaseName}}UpdateDto {
	{{#each properties}}
    {{#unless isSpecialColumn }}
    /**
     * {{documentation}}
     */
    @IsOptional()
    {{name}}?: {{{type.value}}};
    {{/unless}}
	{{/each}}
}
